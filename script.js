const menu=document.querySelector('.menu');const nav=document.querySelector('.header nav');menu?.addEventListener('click',()=>{const open=nav.classList.toggle('open');menu.setAttribute('aria-expanded',String(open))});nav?.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>{nav.classList.remove('open');menu?.setAttribute('aria-expanded','false')}));const track=(name)=>{if(typeof window.gtag==='function')window.gtag('event',name);};document.querySelectorAll('[data-track]').forEach(el=>el.addEventListener('click',()=>track(el.dataset.track)));const form=document.querySelector('#leadForm');form?.addEventListener('submit',e=>{e.preventDefault();const d=new FormData(form);const msg=`Hi Nashy Trellis, I'd like a free quote.%0A%0AName: ${encodeURIComponent(d.get('name'))}%0ASuburb: ${encodeURIComponent(d.get('suburb'))}%0AProduct: ${encodeURIComponent(d.get('product'))}%0AFinish: ${encodeURIComponent(d.get('finish'))}%0A%0AI can send photos of the opening here.`;track('quote_form_to_whatsapp');window.location.href=`https://wa.me/27611792535?text=${msg}`});
const reveal=document.querySelectorAll('.section-intro,.product-grid article,.project-grid figure,.why-copy,.guarantee,.steps>div,.lead-form,.finish-row>div,.faq-list details,.quote-copy');if('IntersectionObserver'in window){const io=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('show');io.unobserve(entry.target)}}),{threshold:.08,rootMargin:'0px 0px -35px'});reveal.forEach(el=>{el.classList.add('reveal');io.observe(el)})}else reveal.forEach(el=>el.classList.add('show'));
const root=document.documentElement;let ticking=false;const updateScroll=()=>{const max=root.scrollHeight-innerHeight;root.style.setProperty('--scroll',max>0?`${Math.min(100,(scrollY/max)*100)}%`:'0%');ticking=false};addEventListener('scroll',()=>{if(!ticking){requestAnimationFrame(updateScroll);ticking=true}},{passive:true});updateScroll();
const spotlight=document.querySelector('.fx-spotlight');if(spotlight&&matchMedia('(pointer:fine)').matches&&!matchMedia('(prefers-reduced-motion:reduce)').matches){addEventListener('pointermove',e=>{root.style.setProperty('--mx',`${e.clientX}px`);root.style.setProperty('--my',`${e.clientY}px`);spotlight.classList.add('on')},{passive:true});document.documentElement.addEventListener('mouseleave',()=>spotlight.classList.remove('on'))}
if(!matchMedia('(prefers-reduced-motion:reduce)').matches){const hero=document.querySelector('.hero-visual');if(hero&&matchMedia('(pointer:fine)').matches){hero.addEventListener('pointermove',e=>{const r=hero.getBoundingClientRect(),x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;hero.style.transform=`perspective(1000px) rotateY(${x*1.8}deg) rotateX(${-y*1.5}deg)`});hero.addEventListener('pointerleave',()=>hero.style.transform='')}}